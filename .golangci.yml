version: "2"

linters:
  default: standard
  disable:
    - errcheck
  settings:
    # v1 CI ran the SA checks only (gosimple/stylecheck were separate, unenabled
    # linters); keep that surface after the v2 merge.
    staticcheck:
      checks: ["SA*"]
