services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${PORT:-8090}:8080"
    environment:
      PORT: "8080"
      NAME: "${NAME:-api / docker}"
      COLOR: "${COLOR:-#1a0a2e,#3d1659,#6b21a8}"
